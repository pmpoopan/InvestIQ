"""Metadata tagging for chunks and source documents.

Phase 1 will attach document type, AMC/scheme identifiers, section labels,
page numbers, and other fields used by retrieval filters and citations.
"""


def tag_metadata(document: object, chunks: list) -> list:
    """Attach metadata to a document and its chunks.

    TODO(Phase 1): implement metadata tagging.
    """
    pass
