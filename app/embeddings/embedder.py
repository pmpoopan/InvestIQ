"""Embedding models and batch encoding for chunks.

Phase 2 will wrap sentence-transformers (or equivalent) so chunk vectors can
be stored and queried consistently across ablation runs.
"""


def embed_texts(texts: list[str]) -> list:
    """Encode texts into dense vectors.

    TODO(Phase 2): implement embedding.
    """
    pass
